<Application x:Class="CommunityBookLovers.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CommunityBookLovers"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Стиль для кнопки-ссылки -->
        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#FF8B5F65"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" 
                               TextDecorations="Underline"
                               Foreground="{TemplateBinding Foreground}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FF6A4A4A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для обычных кнопок -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF8B5F65"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#40000000"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF6A4A4A"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF4A2F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
            <!-- Общие стили -->
            <Style x:Key="SectionBlock" TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Margin" Value="0,0,0,20"/>
                <Setter Property="Padding" Value="20"/>
                <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#10000000"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="SectionTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#FF8B5F65"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
            </Style>

            <Style x:Key="StatCard" TargetType="Border">
                <Setter Property="Background" Value="#FFF5F5F5"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="15"/>
                <Setter Property="Margin" Value="10"/>
            </Style>

            <Style x:Key="StatTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="#666"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
            </Style>

            <Style x:Key="StatValue" TargetType="TextBlock">
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#FF8B5F65"/>
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="0,5,0,0"/>
            </Style>

            <!-- Шаблоны элементов -->
            <DataTemplate x:Key="BookItemTemplate">
                <Border Width="180" Height="250" Margin="10" Background="White"
                BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Обложка книги -->
                        <Border Grid.Row="0" Background="#FFF5F5F5" CornerRadius="5,5,0,0">
                            <Image Source="{Binding ImagePath}" Stretch="Uniform" MaxHeight="150"/>
                        </Border>

                        <!-- Информация о книге -->
                        <StackPanel Grid.Row="1" Margin="10">
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" 
                               Foreground="#FF8B5F65" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Author.Name}" FontStyle="Italic"
                               Foreground="#FF8B5F65" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="ActivityItemTemplate">
                <Border Width="220" Height="80" Margin="10" Background="#FFF5F5F5"
                CornerRadius="5" Padding="10">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding UserImage}" Width="50" Height="50" 
                       Stretch="UniformToFill" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Text="{Binding UserName}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Action}" FontSize="12" Foreground="#666"/>
                            <TextBlock Text="{Binding BookTitle}" FontStyle="Italic"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </DataTemplate>

            <!-- Панели для горизонтального расположения -->
            <ItemsPanelTemplate x:Key="HorizontalItemsPanel">
                <StackPanel Orientation="Horizontal" Margin="0,10"/>
            </ItemsPanelTemplate>
        
    </Application.Resources>
    
</Application>
