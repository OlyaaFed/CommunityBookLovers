<Window x:Class="CommunityBookLovers.LWindow.ReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommunityBookLovers.LWindow"
        xmlns:wpfAnimatedGif="http://wpfanimatedgif.codeplex.com"
        mc:Ignorable="d"
        Title="Отзывы о книгах" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#FF2E1B2A">
    <!-- Темно-бордовый фон -->

    <Window.Resources>
        <!-- Стиль для кнопок -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF8B5F65"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="#FF8B5F65"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#FF8B5F65" Opacity="0.5" BlurRadius="10" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Верхняя панель с меню -->
        <Grid DockPanel.Dock="Top" Background="#FF8B5F65">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Menu Grid.Column="0" Margin="0,0,0,25" Background="#FF8B5F65" Foreground="White">
                <MenuItem Header="О себе" FontFamily="Segoe Print" Click="MenuItem_AboutMe_Click" Width="57" />
                <MenuItem Header="Своя полка" FontFamily="Segoe Print" Click="MenuItem_MyShelf_Click" />
                <MenuItem Header="Книги" FontFamily="Segoe Print" Click="MenuItem_Books_Click" />
                <MenuItem Header="Друзья" FontFamily="Segoe Print" Click="MenuItem_Friends_Click" />
                <MenuItem Header="Отзывы" FontFamily="Segoe Print" Click="MenuItem_Review_Click" />
                <MenuItem Header="Главная" Click="MenuItem_Home_Click" FontFamily="Segoe UI" FontWeight="Bold"/>
            </Menu>

            <Button Grid.Column="1" Content="Выйти" Width="80" Height="30" Click="LogoutButton_Click" Background="#FF8B5F65" Foreground="White" FontWeight="Bold" Cursor="Hand" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10"/>
        </Grid>

        <!-- Основной контент -->
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- Левая панель: Оставить отзыв -->
            <Border Grid.Column="0" Background="#FFF5F5F5" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="Оставить отзыв" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="#FF8B5F65"/>
                    <Label Content="Выберите книгу" Foreground="#FF8B5F65"/>
                    <ComboBox x:Name="BookComboBox" DisplayMemberPath="Title" SelectedValuePath="BookId" Background="#FFF5F5F5" Foreground="#FF8B5F65"/>

                    <Label Content="Оценка (1-5)" Margin="0,10,0,0" Foreground="#FF8B5F65"/>
                    <ComboBox x:Name="RatingComboBox" Background="#FFF5F5F5" Foreground="#FF8B5F65">
                        <ComboBoxItem Content="1" />
                        <ComboBoxItem Content="2" />
                        <ComboBoxItem Content="3" />
                        <ComboBoxItem Content="4" />
                        <ComboBoxItem Content="5" />
                    </ComboBox>

                    <Label Content="Текст отзыва" Margin="0,10,0,0" Foreground="#FF8B5F65"/>
                    <TextBox x:Name="ReviewTextBox" AcceptsReturn="True" Height="100" TextWrapping="Wrap" Background="#FFF5F5F5" Foreground="#FF8B5F65"/>

                    <Button Content="Сохранить отзыв" Margin="0,10,0,0" Click="SaveReviewButton_Click"/>
                </StackPanel>
            </Border>

            <!-- Правая панель: Отзывы и фильтрация -->
            <Border Grid.Column="1" Background="#FFF5F5F5" CornerRadius="10" Padding="15" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Отзывы" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="#FF8B5F65"/>

                    <!-- Фильтрация -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <ComboBox x:Name="FilterBookComboBox" 
              DisplayMemberPath="Title" 
              SelectedValuePath="BookId" 
              Width="200" 
              Margin="0,0,10,0" 
              Background="#FFF5F5F5" 
              Foreground="#FF8B5F65"/>
                        <DatePicker x:Name="FilterDatePicker" 
                Width="150" 
                Margin="0,0,10,0" 
                Background="#FFF5F5F5" 
                Foreground="#FF8B5F65"/>
                        <Button Content="Фильтровать" Click="FilterReviewsButton_Click"/>
                        <Button Content="Сбросить" Click="ResetFilterButton_Click" Margin="10,0,0,0"/>
                    </StackPanel>

                    <!-- Список отзывов -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="300">
                        <ListBox x:Name="ReviewListBox" SelectionChanged="ReviewListBox_SelectionChanged" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderThickness="0,0,0,1" BorderBrush="Gray" Padding="5">
                                        <StackPanel>
                                            <TextBlock FontWeight="Bold" Text="{Binding Book.Title}" Foreground="#FF8B5F65"/>
                                            <TextBlock Text="Пользователь: " FontWeight="SemiBold" Margin="5,0,0,0" Foreground="#FF8B5F65">
                                                <Run Text="{Binding User.Name}" Foreground="#FF8B5F65"/>
                                            </TextBlock>
                                            <TextBlock Text="Оценка: " FontWeight="SemiBold" Margin="5,0,0,0" Foreground="#FF8B5F65">
                                                <Run Text="{Binding Rating}" Foreground="#FF8B5F65"/>
                                            </TextBlock>
                                            <TextBlock TextWrapping="Wrap" Text="{Binding Text}" MaxWidth="400" Margin="0,5,0,0" Foreground="#FF8B5F65"/>
                                            <TextBlock FontStyle="Italic" FontSize="10" Text="{Binding Date}" HorizontalAlignment="Right" Foreground="#FF8B5F65"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>